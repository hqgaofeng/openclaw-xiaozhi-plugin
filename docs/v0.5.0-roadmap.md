# v0.5.0 路线图(待 Allen 拍板)

> **状态**:草案 v1 — 子 agent 起草,Allen 审完手动 `git add` / `git commit` / `git push`。
> **起草时间**:2026-06-10 20:06 GMT+8
> **起草依据**:`d665bd2` (v0.4.0 tag) + 5 个 RC commit + CHANGELOG.md + 25 测试文件 / 330 it() runs

---

## 1. 状态回顾:v0.4.0 已完成

**事实核对(以 git/CHANGELOG 为准,不是以任务 brief 为准)**

| 维度 | 实际状态 | 任务 brief 描述 | 备注 |
|---|---|---|---|
| HEAD | `d665bd2` | `d665bd2` | ✅ 一致 |
| Tag | `v0.4.0` 已打 | `v0.4.0` 已打 + push | ✅ 一致 |
| 功能数 | **9** (CHANGELOG "Added" §1-§9) | "12 项官方对齐重构" | ⚠️ commit message 写 12,CHANGELOG 实际列 9;**以 CHANGELOG 9 为准** |
| 5 个 RC commit | rc1-rc5 全部已在 v0.4.0 tag 内 | "5 commits ahead 还没合并" | ⚠️ `git rev-list --left-right HEAD...origin/main` = `0 0`,**没有未 push 提交** |
| 测试 | 25 文件 / **330 it() runs** | 未提 | CHANGELOG 报 184 baseline + 新加 = 330,核实通过 |
| VPS 状态 | mem 961Mi / used 849Mi / free 78Mi / swap 2.0Gi | "961MiB RAM" | ✅ 一致(本机即 VPS) |

**v0.4.0 收口的 9 个功能**(CHANGELOG §[v0.4.0] - Added,顺序排列):

| # | 功能 | 开关 | 关键文件 | 测试数(估算) |
|---|---|---|---|---|
| 1 | Streaming TTS pipeline | `useStreamingTts` | `src/ttsPipeline.ts` | 13 |
| 2 | Text cleaner(MarkdownCleaner + replace_words + IncrementalCleaner) | 与 #1 捆绑 | `src/textCleaner.ts` | 26 |
| 3 | Metrics module(Counter/Histogram/Gauge, JSON 导出) | `metricsEnabled` | `src/metrics.ts` | 20 |
| 4 | Silero ONNX VAD(server-side) | `useSileroVad` | `src/vad-silero.ts` | 11 |
| 5 | Streaming ASR(OnlineRecognizer pull-based) | `useStreamingAsr` | `src/asr/sherpa-onnx-streaming.ts` | 16 |
| 6 | PCM accumulation(opus decode 即时, 不再 buffer 整段) | `useAccumulatePcm` | `src/inbound.ts` | 6(在 inbound test 内) |
| 7 | Multi-flag state machine(wakeupPrimed/llmStreaming/ttsAbort/metricsTick) | `useMultiFlagState` | `src/session-flags.ts` | 15 |
| 8 | OAuth multi-device(Bearer + per-device store) | `useOAuth` | `src/oauth/{client,middleware,store,types}.ts` | 29(3 子文件) |
| 9 | Retry helper(exponential backoff + jitter) | `useRetry` | `src/retry.ts` | 16 |
| | | | **合计** | **~152 新增(184 baseline + 152 ≈ 336,跟 330 接近)** |

**风险点(v0.4.0 自报,延续到 v0.5.0)**:
- `onnxruntime-node` native binding 加载时多 ~80MB(已在 2Gi swap 范围)
- OAuth token store 默认 in-memory,重启丢 — 计划放 v0.5.0 持久化
- Retry 副作用:退避可能撞 LLM stream deadline,默认 maxRetries=3

---

## 2. v0.5.0 候选特性池(Allen 拍板)

> **数据校正**:任务 brief 给的"工作量/风险/依赖"是子 agent 估算,**跟实际代码现状可能有偏差**(见每行 ⚠️ 标注)。最终以 Allen 拍的为准。

### A. 功能扩展

| # | 特性 | 价值 | 工作量 | 风险 | 依赖 | 推荐 | 备注/校正 |
|---|---|---|---|---|---|---|---|
| 1 | 多设备 P2P 同步 | 中 | 1-2 周 | 高 | OAuth 已 OK(per-device store) | P2 | OAuth 现仅是 token 隔离,**P2P sync = CRDT/状态合并**,工作量大;建议先做 token 持久化(见 B-1)再谈 P2P |
| 2 | MCP 工具市场(开源社区) | 高 | 2-3 周 | 中 | `src/mcp/registry.ts` 已有(esp32 device tool) | P1 | 现 registry 只接 esp32 自报工具;扩成"npm-installable 第三方工具"需:schema 校验 + 沙箱 + 版本管理 |
| 3 | OTA 增量更新 | 中 | 1 周 | 低 | `src/ota.ts` 168 行已实现 | P2 | 现 OTA 返 WS URL + version;增量 = diff firmware,需 esp32 端配合,服务端工作小 |
| 4 | 多 LLM provider (Anthropic/OpenAI/Ollama) | 高 | 3-4 天 | 低 | **LLM provider 抽象当前不存在** | P1 | ⚠️ 校正:`src/api.ts` 现有 `asr.provider` / `tts.provider` 联合类型,**没有 LLM provider 抽象**。先抽 interface → 加 3 个 provider → 默认走 openclaw 自身 dispatch |
| 5 | LLM streaming(实时 token 输出) | 高 | 2-3 天 | 中 | `dispatchInboundDirectDmWithRuntime`(`src/inbound.ts:835`) | P1 | 现 agent 走 batch reply;text-only fallback 已 M3.7.3 落;真 streaming 需 SDK 支持 token 回调 |
| 6 | ESP32-S3 / ESP32-C3 适配 | 中 | 3-4 天 | 低 | **仓内无 sdkconfig** | P3 | ⚠️ 校正:`find . -name "sdkconfig*"` 0 hit,firmware 在另一仓;服务端工作小,真适配在 firmware 仓 |
| 7 | 唤醒词自定义(训练 pipeline) | 中 | 1-2 周 | 高 | **仓内无 sherpa-onnx kws 模块** | P3 | ⚠️ 校正:`ls src/` 无 kws/wake 文件;sherpa-onnx npm 包有 KWS 但本项目没集成;先集成 KWS 再谈训练 |
| 8 | Voice activity logging(音频片段保存) | 中 | 3-5 天 | 低 | audio buffer(`src/audio.ts` OpusCodec) | P2 | 复用 OpusCodec 解码 → 写 wav/mp3;注意 GDPR/隐私开关 |
| 9 | 离线 TTS(本地 neural) | 高 | 2-3 周 | 高 | **无可用模型**;RAM 风险大 | P3 | 当前 VPS free=78Mi,本地 neural TTS 模型 ≥200MB 必 OOM;建议 v0.6.0+ 配合硬件升级 |
| 10 | 多语言 ASR(粤语/日语/法语) | 中 | 1 周 | 中 | sherpa-onnx 多语言模型齐全 | P2 | 现 sherpa-onnx int8 是 zh+en,加粤语需换/扩模型 |

### B. 基础设施(必然要做 — 这是 P1 默认盘)

| # | 项 | 价值 | 工作量 | 推荐 | 备注/校正 |
|---|---|---|---|---|---|
| 1 | Docker Compose prod 部署 | 高 | 2-3 天 | **P1 必含** | 现仓**没有 Dockerfile / compose.yaml**;需新写,目标镜像 < 400MB |
| 2 | Prometheus / OTel exporter | 高 | 1 周 | P2 | `src/metrics.ts` 已 JSON 导出,加 Prom format 是 1-2 天 wrapper;OTel SDK 是 1 周 |
| 3 | 灰度开关 dashboard (web UI) | 中 | 1 周 | P3 | 现 8 个开关都是 JSON 改,加 web UI = React 简单页 + REST;ROI 不高 |
| 4 | 自动化 e2e (esp32 模拟) | 高 | 2 周 | P2 | 现 25 个单元/集成测试,**0 个 e2e**;用 ws client 模拟 esp32 跑通 hello→listen→tts 即可 |
| 5 | GitHub Actions CI (vitest + tsc + build) | 高 | 1-2 天 | **P1 必含** | 现**无 CI 配置**(`.github/workflows/` 0 文件);CI 绿是 v0.5.0 唯一硬交付门 |
| 6 | npm publish 自动化 | 中 | 1 天 | P2 | 现是 `openclaw.plugin.json` 私仓,走 npm publish 需先确认商业模式 |

### C. 技术债清理(Allen 拍板:可放 v0.6.0)

| # | 项 | 价值 | 工作量 | 备注/校正 |
|---|---|---|---|---|
| 1 | 大文件拆分 | 高 | 3-5 天 | ⚠️ 校正:`src/inbound.ts` = **1047 行**(不是 900);`src/metrics.ts` 469 / `src/vad-silero.ts` 345 / `src/gateway.ts` 345 也偏大 |
| 2 | TypeScript strict mode 完整开 | 中 | 2-3 天 | 现 `tsconfig.json` strict 部分开;开全要修 ~50 处 `any`,可分批开(`strictNullChecks` → `noImplicitAny` → `strict`) |
| 3 | 错误处理统一 (Result<T, E> 模式) | 中 | 3-4 天 | 现错误处理混杂(throw + return null + console.error);抽 `Result<T, E>` + `Result.try()` helper |
| 4 | 测试覆盖率 80% → 90% | 中 | 持续 | 现 vitest `--coverage` 未配(`vitest.config.ts` 1125 bytes);覆盖率数据无;先把覆盖率脚本跑出 baseline |

---

## 3. v0.5.0 建议范围(子 agent 起草,Allen 拍)

### 3.1 子 agent 推荐配置(对照 brief 调整)

| 类别 | 项 | 来源 | 推荐度 | 备注 |
|---|---|---|---|---|
| **P1 必含** | 多 LLM provider | A-4 | ✅ 必含 | 3-4 天可完;**前置抽 LLM provider interface** |
| **P1 必含** | LLM streaming | A-5 | ✅ 必含 | 2-3 天,跟 A-4 共用一个 dispatch 改动 |
| **P1 必含** | Docker Compose prod | B-1 | ✅ 必含 | 2-3 天,部署前提 |
| **P1 必含** | GitHub Actions CI | B-5 | ✅ 必含 | 1-2 天,CI 绿 = 交付门 |
| **P1 加项** | MCP 工具市场(开源) | A-2 | ⚠️ 建议放 v0.5.1 | 工作量超 2 周,跟 P1 必含抢周期 |
| **P2 加 1** | 自动化 e2e (esp32 mock) | B-4 | ✅ 建议加 | 2 周,跟 CI 联动 |
| **P2 加 2** | 多语言 ASR | A-10 | ✅ 建议加 | 1 周,模型现成,sherpa-onnx 一行配置 |
| **不建议 v0.5.0 同期** | P2/P3 其余项 | — | 排 v0.5.1+ | 5+ 项 = 跟 v0.4.0 12 项撞坑,留 buffer 给测 |

### 3.2 拍板选项(Allen 选一)

| 选项 | 内容 | 子 agent 评估 |
|---|---|---|
| **A. 保守** | 4 个 P1 必含,不加 P2 | 风险低,~2 周可完;错过 e2e + 多语言 ROI |
| **B. 平衡(推荐)** | 4 个 P1 必含 + e2e(B-4) | 风险中,~3-4 周;**e2e 是 v0.5.0 长期 ROI 最高的 P2** |
| **C. 激进** | 4 个 P1 必含 + e2e + 多语言 ASR | 风险中-高,~4-5 周;**不推荐**:跟 v0.4.0 12 项同期搞撞坑历史复现(见 §4.3) |

> **子 agent 倾向**:选项 B(平衡)。

---

## 4. 风险评估

### 4.1 资源风险

| 资源 | 当前 | 风险 | 缓解 |
|---|---|---|---|
| VPS RAM total | 961MiB | high | onnxruntime-node 已吃 ~80MB;**新功能默认 opt-in**,灰度开关按需开 |
| VPS RAM free | 78MiB | high | swap 2.0Gi(当前 used 994Mi / free 1.0Gi);加 native 模块前先 `npm install --dry-run` 评估 |
| Disk / | 充裕(2.5GB+ free 常见) | low | OTA 模型 + sherpa-onnx 模型 + Silero 模型合计 < 50MB |
| npm registry | 公开 | low | 无限制 |

**硬性约束(写进开发流程)**:**任何新增 native binding(`onnxruntime-node` / `better-sqlite3` / `sharp` 等)前,先在本地 `npm install --dry-run --json` 看装后体积,再决策**。

### 4.2 Git 状态风险

| 状态 | 实际 | 任务 brief 描述 | 校正 |
|---|---|---|---|
| 未 push commits | **0**(`git rev-list HEAD...origin/main` = `0 0`) | "5 commits ahead 还没合并" | ⚠️ brief 错;v0.4.0 tag 已包含全部 5 个 RC commit |
| 工作区状态 | clean | (隐含 clean) | ✅ |
| Untracked 文件 | 0 | (隐含 0) | ✅ |

**结论**:`main` 与 `origin/main` 完全同步,**v0.5.0 可以从干净状态直接开干**,无需先合并任何 backlog。

### 4.3 撞坑历史(防止 v0.5.0 重演 v0.4.0 12 项同期搞的踩坑)

来自 `~/.openclaw/workspace/memory/2026-06-08.md` + `2026-06-10.md`:

- **§4.45 git tag force 替换**:**第一次打 tag 必带 `-m`**,避免二次 fix
- **§4.46 v0.3.0 是第一个正式 release**:v0.x.y 中 `y` 是修 bug,`.0` 是收口;**v0.5.0 也应按"收口 release"标准**
- **§4.47 同一端口 HTTP + WS**:本项目已用(18790 端口),**v0.5.0 不要回退到 2 端口**
- **§4.48 测试套的价值**:4 个测试 case 揭穿"V2 行为跟文档印象 4 处不同" → **v0.5.0 任何"我以为 SDK 行为是 X"都要先写测试验证,不能凭印象改 dispatch**

### 4.4 子 agent 教训(Allen 6/3 18:30 拍板 — 写进开发流程)

来源:AGENTS.md Hard Rules #1-3:

1. **不编造** — 不确定就说"不确定"或"让我先查",严禁用"我猜"填空
2. **协商沟通** — 重大决策前**先列选项 + 推荐 + 理由**,让用户选(本路线图就是该模式)
3. **依据代码事实** — 所有判断查文件 / git log / API,不靠猜(本路线图所有"工作量/风险"校正都标 ⚠️)
4. **文档及时** — 计划里说要更新的文档,**代码 commit 之后立刻做 docs commit**
5. **测试纪律** — 改前先跑测试看基线,改后跑确认;**CI 绿是唯一合法交付门**

---

## 5. 里程碑预估(Allen 拍板后填日期)

> **占位**:以下日期是**子 agent 按"选项 B 工作量 + Allen 排期习惯"估算**,Allen 拍板时调整。

| 里程碑 | 含义 | 预计日期(选项 B) | Gate |
|---|---|---|---|
| v0.5.0-rc1 | 4 个 P1 必含功能冻结(feature freeze) | 2026-06-24(选项 B 起点 + 2 周) | CI 绿 + 4 项 P1 单测覆盖 + docs/CHANGELOG 同步 |
| v0.5.0-rc2 | 内部测试(VPS 灰度 + 1 真 esp32 跑通 e2e) | 2026-07-01(+ 1 周) | e2e 跑通 + Allen 实测 1 台 esp32 + 内存 < 850Mi 持续 24h |
| v0.5.0 | 正式发布(tag + push + release notes) | 2026-07-08(+ 1 周) | rc2 跑过 7 天无 P0/P1 bug + 4 P1 + 1 P2(e2e)合并 |

**前提**:Allen 拍板后 v0.5.0 工作**立即开始**(2026-06-10 ~ 06-11);若拍板晚于 06-15,里程碑顺延。

---

## 6. 待 Allen 决策的 4 个问题

1. **范围**:选 A(保守) / B(平衡-推荐) / C(激进)?
2. **优先级重排**:A-4(多 LLM provider)是否真 P1?若 Allen 觉得"openclaw 自带 dispatch 已够",可降到 P2
3. **CI 平台**:GitHub Actions(本仓已在 github.com) / GitLab CI / 其它?
4. **发布模式**:v0.5.0 走 npm publish 公开 / 继续私仓 `openclaw.plugin.json` 拉取?

---

## 7. 不要做的清单(再次强调)

- ❌ **不 commit / 不 push**(本路线图是文档草稿,Allen 审完手动)
- ❌ **不动 src/**(本任务不涉及代码)
- ❌ **不擅自把"待定"项填成"已选"**(本路线图所有 ❓ 标"待 Allen 拍")
- ❌ **不重打 v0.4.0 tag**(已存在,`git tag -l` 显示)

---

## 8. 附录:数据来源

| 数据 | 来源文件 | 取数命令 |
|---|---|---|
| HEAD = `d665bd2` | git | `git rev-parse HEAD` |
| Tag 列表 | git | `git tag -l` |
| Commits ahead/behind = 0/0 | git | `git rev-list --left-right HEAD...origin/main` |
| v0.4.0 功能清单(9 项) | `CHANGELOG.md` §[v0.4.0] - Added | `read` |
| 25 测试文件 / 330 it() | `src/tests/*.test.ts` | `find src -name "*.test.ts" \| xargs grep -c "^\s*it("` |
| `inbound.ts` 1047 行 | `src/inbound.ts` | `wc -l src/inbound.ts` |
| VPS 内存 961Mi / free 78Mi | `/proc/meminfo` | `free -h` |
| `dispatchInboundDirectDmWithRuntime` 位置 | `src/inbound.ts:835` + `src/handle/esp32ListenHandler.ts:219` | `grep -rn` |
| 现有 MCP registry 范围 | `src/mcp/registry.ts` 56 行 | `read` |
| 现有 OTA 范围 | `src/ota.ts` 168 行 | `wc -l src/ota.ts` |
| §4.45-§4.48 教训 | `~/.openclaw/workspace/memory/2026-06-08.md:140-240` | `read` |
| AGENTS.md Hard Rules | `~/.openclaw/workspace/AGENTS.md` 底部 | `read` |

---

**草案结束 — Allen 审。**
